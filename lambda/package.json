{
  "name": "fast-food-auth-lambda",
  "version": "1.0.0",
  "description": "Função Lambda para autenticação do cliente Fast Food",
  "main": "src/functions/auth/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "start-api": "sam local start-api"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.767.0",
    "@aws-sdk/lib-dynamodb": "^3.767.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^3.1.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "src/**/*.js"
    ]
  }
}
